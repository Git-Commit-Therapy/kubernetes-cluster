apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-backend-patients-deployment
  labels:
    app: java-backend-patients

spec:
  selector:
    matchLabels:
      app: java-backend-patients
  template:
    metadata:
      labels:
        app: java-backend-patients
    spec:
      containers:
        - name: java-backend-patients
          image: ghcr.io/git-commit-therapy/java-backend-patients:latest
          ports:
            - containerPort: 443
          envFrom:
            - configMapRef:
                name: java-backend-patients-configmap
            - secretRef:
                name: java-backend-patients-secret
          volumeMounts:
            - mountPath: /certs
              name: tls-certificate
            - mountPath: /trust
              name: trust-certificates
      volumes:
        - name: tls-certificate
          secret:
            secretName: java-backend-patients-certificate-secret
        - name: trust-certificates
          configMap:
            name: root-trust-bundle

      imagePullSecrets:
        - name: regcred

